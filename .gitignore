# Compiled TypeScript output (only in infra folders)
bin/*.js
bin/*.d.ts
lib/*.js
lib/*.d.ts

# Dependencies
node_modules

# CDK asset staging directory
.cdk.staging
cdk.out

# Keep one lockfile only (pnpm). Uncomment to ignore npm's lockfile:
# package-lock.json